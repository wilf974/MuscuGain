# P2 Coach IA — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Transformer MuscuGain en coach : (1) bilan IA de l'historique, (2) génération de programme par IA, (3) croisement corps×training, (4) suggestion de charge. L'IA digère le JSON ; l'utilisateur lit du conseil.

**Architecture:** 2 nouveaux endpoints backend (`/coach-analysis`, `/generate-program`, réutilisent NVIDIA NIM `MODEL`, texte seul). Front : util pur `coach.core.js` (résumé compact de l'historique + normalisation, testé) + IO `coach.js`, carte bilan sur Dashboard (cache 1/jour), `GenerateProgramModal`, suggestion de charge front-only dans Workout. Croisement corps×training = l'analyse corporelle passée en option à `/coach-analysis`.

**Tech Stack:** Fastify ESM, React 19, Tailwind 3, lucide-react, `node --test`. Gate `npm run build`.

---

## File Structure
| Fichier | Action | Responsabilité |
|---------|--------|----------------|
| `backend/server.js` | Modifier | `POST /coach-analysis`, `POST /generate-program` |
| `src/utils/coach.core.js` | Créer | `buildHistorySummary`, `normalizeCoachAnalysis`, `normalizeGeneratedProgram`, `suggestLoad` (purs) |
| `src/utils/coach.core.test.js` | Créer | Tests node:test |
| `src/utils/coach.js` | Créer | IO : `fetchCoachAnalysis`, `generateProgram`, `CoachError` |
| `src/data/exercises.js` | Modifier | exporter `EXERCISE_CATEGORY` (map nom→catégorie) si utile au résumé |
| `src/views/Dashboard.jsx` | Modifier | Carte bilan IA (bouton + cache + affichage), bouton « Générer par IA » |
| `src/components/modals/GenerateProgramModal.jsx` | Créer | Objectif → génération → aperçu → enregistrer |
| `src/views/Workout.jsx` | Modifier | Suggestion de charge (chip tap-to-fill) |
| `src/App.jsx` | Modifier | wiring modal génération + `addGeneratedRoutine`, cache bilan |

Clés localStorage : `muscuGainCoachAnalysis` = `{ date:'YYYY-MM-DD', data:{...} }` (bilan du jour).

---

### Task 1: `coach.core.js` — résumé + normalisations + suggestLoad (TDD)

**Files:**
- Create: `src/utils/coach.core.js`, `src/utils/coach.core.test.js`

**Spec des fonctions pures :**
- `buildHistorySummary(history, categoryOf)` → objet compact borné :
  - `categoryOf` : `(exName) => 'chest'|'back'|...|'other'` (injecté pour rester pur/testable).
  - prend les **30 dernières** séances ; calcule : `totalSessions`, `dateFrom`/`dateTo` (ISO), `perExercise` = pour chaque exercice vu : `{ name, sessions, maxWeight, lastWeight, lastReps, trend }` (`trend` = 'up'|'down'|'flat' selon premier vs dernier maxWeight `done`), trié par `sessions` desc et **coupé à 15** ; `muscleVolume` = somme du volume `done` par catégorie ; `weeklyFrequency` = sessions / nb semaines couvertes (arrondi 1 décimale).
  - poids/reps via `Number()` sur sets `done`. Jamais throw ; history vide → `{ totalSessions:0, perExercise:[], muscleVolume:{}, weeklyFrequency:0 }`.
- `normalizeCoachAnalysis(json)` → `{ overview:'', progression:[], plateaus:[], weeklyVolume:'', balance:'', deload:'', bodyCross:'' }` (strings trim, arrays nettoyés/cap 6). null-safe.
- `normalizeGeneratedProgram(json, validNames)` → `{ name:'', exercises:[{name,targetSets,targetReps,restSeconds}] }` : ne garde que les exercices dont `name` ∈ `validNames` (Set/array), `targetSets`/`targetReps` = `Math.max(1, parseInt||3/10)`, `restSeconds` = parseInt>0 ou omis ; cap 12 exos. null-safe → `{name:'', exercises:[]}`.
- `suggestLoad(lastLog, targetReps)` → `{ weight, bump }` : si pas de `lastLog` ou poids non numérique → `null`. Sinon `w = Number(lastLog.weight)` ; si `Number(lastLog.reps) >= Number(targetReps||0)` et w>0 → `{ weight: round(w+2.5), bump:true }` sinon `{ weight: w, bump:false }`. `round` = arrondi 0.5 le plus proche.

- [ ] **Step 1: Write failing tests** `src/utils/coach.core.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildHistorySummary, normalizeCoachAnalysis, normalizeGeneratedProgram, suggestLoad } from './coach.core.js';

const cat = (n) => (n === 'Squat' ? 'legs' : n === 'Bench' ? 'chest' : 'other');
const H = [
  { date: '2026-02-01T10:00:00.000Z', exercises: { Squat: [{ weight: '100', reps: '5', done: true }], Bench: [{ weight: '60', reps: '8', done: true }] } },
  { date: '2026-02-08T10:00:00.000Z', exercises: { Squat: [{ weight: '110', reps: '5', done: true }] } },
];

test('buildHistorySummary aggregates per exercise with trend', () => {
  const s = buildHistorySummary(H, cat);
  assert.equal(s.totalSessions, 2);
  const sq = s.perExercise.find((e) => e.name === 'Squat');
  assert.equal(sq.maxWeight, 110);
  assert.equal(sq.sessions, 2);
  assert.equal(sq.trend, 'up');
  assert.ok(s.muscleVolume.legs > 0);
});

test('buildHistorySummary empty-safe', () => {
  const s = buildHistorySummary([], cat);
  assert.equal(s.totalSessions, 0);
  assert.deepEqual(s.perExercise, []);
});

test('normalizeCoachAnalysis null-safe + caps arrays', () => {
  const r = normalizeCoachAnalysis(null);
  assert.equal(r.overview, '');
  assert.deepEqual(r.progression, []);
  const big = normalizeCoachAnalysis({ plateaus: ['a','b','c','d','e','f','g'] });
  assert.equal(big.plateaus.length, 6);
});

test('normalizeGeneratedProgram keeps only valid names', () => {
  const r = normalizeGeneratedProgram({ name: 'Test', exercises: [{ name: 'Squat', targetSets: 5, targetReps: 5 }, { name: 'Inconnu', targetSets: 3 }] }, ['Squat', 'Bench']);
  assert.equal(r.name, 'Test');
  assert.equal(r.exercises.length, 1);
  assert.equal(r.exercises[0].name, 'Squat');
  assert.equal(r.exercises[0].targetSets, 5);
});

test('suggestLoad bumps when reps target met', () => {
  assert.deepEqual(suggestLoad({ weight: '100', reps: '5' }, 5), { weight: 102.5, bump: true });
  assert.deepEqual(suggestLoad({ weight: '100', reps: '3' }, 5), { weight: 100, bump: false });
  assert.equal(suggestLoad(null, 5), null);
});
```

- [ ] **Step 2: Run → fail** `cd /opt/apps/MuscuGain && node --test src/utils/coach.core.test.js` (module not found).

- [ ] **Step 3: Implement `src/utils/coach.core.js`** per the function spec above (pure, no imports). Make every test pass. Key helpers: `num(v)=Number(v)`, guard `Number.isFinite`. `roundHalf(x)=Math.round(x*2)/2`.

- [ ] **Step 4: Run → all pass.**

- [ ] **Step 5: Commit** `git add src/utils/coach.core.js src/utils/coach.core.test.js && git commit -m "feat(muscugain): coach.core résumé historique + normalisations + suggestLoad (TDD)"`

---

### Task 2: Backend — `/coach-analysis` + `/generate-program`

**Files:** Modify `backend/server.js` (two handlers before `app.listen`).

- [ ] **Step 1: Add `/coach-analysis`**

```js
app.post('/coach-analysis', async (req, reply) => {
  if (!API_KEY) return reply.code(503).send({ error: 'Clé NVIDIA non configurée' });
  const { summary, bodyAnalysis } = req.body || {};
  if (!summary || typeof summary !== 'object') return reply.code(400).send({ error: 'résumé manquant' });

  let bodyText = '';
  if (bodyAnalysis && typeof bodyAnalysis === 'object') {
    const b = bodyAnalysis;
    bodyText = `\nAnalyse corporelle récente: morphotype=${b.morphotype || '?'}, faiblesses=${(b.weaknesses || []).join('; ') || '?'}. ` +
      "Croise-la avec l'entraînement réel pour 'bodyCross'.";
  }

  const prompt =
    "Tu es un coach de musculation. Voici un résumé chiffré de l'historique d'entraînement d'une personne (JSON). " +
    "Analyse-le et donne un bilan motivant et concret, NON médical. " +
    "Évalue: progression (exercices qui montent), plateaux (stagnation), volume hebdo, équilibre entre groupes musculaires (push/pull/jambes), et si un deload est utile. " +
    "Réponds UNIQUEMENT en JSON: " +
    '{"overview":"<2 phrases>","progression":["..."],"plateaus":["..."],"weeklyVolume":"<court>","balance":"<court>","deload":"<court>","bodyCross":"<vide si pas d\'analyse corporelle>"}. ' +
    'Résumé: ' + JSON.stringify(summary) + bodyText;

  const payload = {
    model: MODEL,
    messages: [{ role: 'user', content: prompt }],
    max_tokens: 700,
    temperature: 0.3,
  };

  let res;
  try {
    res = await fetch(NVIDIA_URL, {
      method: 'POST',
      headers: { Authorization: `Bearer ${API_KEY}`, 'Content-Type': 'application/json' },
      body: JSON.stringify(payload),
      signal: AbortSignal.timeout(60000),
    });
  } catch (e) {
    req.log.error({ err: String(e) }, 'NVIDIA fetch failed (coach)');
    return reply.code(504).send({ error: 'Délai dépassé côté modèle' });
  }
  if (!res.ok) {
    const t = await res.text().catch(() => '');
    req.log.error({ status: res.status, body: t.slice(0, 300) }, 'NVIDIA error (coach)');
    return reply.code(502).send({ error: 'Erreur du modèle', status: res.status });
  }
  const out = await res.json();
  const parsed = extractJson(out?.choices?.[0]?.message?.content || '');
  if (!parsed) return reply.send({});
  const str = (v) => (typeof v === 'string' ? v.trim() : '');
  const arr = (v) => (Array.isArray(v) ? v.filter((x) => typeof x === 'string' && x.trim()).map((x) => x.trim()).slice(0, 6) : []);
  return reply.send({
    overview: str(parsed.overview), progression: arr(parsed.progression), plateaus: arr(parsed.plateaus),
    weeklyVolume: str(parsed.weeklyVolume), balance: str(parsed.balance), deload: str(parsed.deload), bodyCross: str(parsed.bodyCross),
  });
});
```

- [ ] **Step 2: Add `/generate-program`**

```js
app.post('/generate-program', async (req, reply) => {
  if (!API_KEY) return reply.code(503).send({ error: 'Clé NVIDIA non configurée' });
  const { objective, catalog } = req.body || {};
  if (!objective || typeof objective !== 'string') return reply.code(400).send({ error: 'objectif manquant' });

  const names = catalog && typeof catalog === 'object'
    ? Object.values(catalog).flat().filter((x) => typeof x === 'string')
    : [];
  if (!names.length) return reply.code(400).send({ error: 'catalogue manquant' });

  const prompt =
    "Tu es un coach de musculation. Crée un programme adapté à cet objectif: \"" + objective.slice(0, 300) + "\". " +
    "Choisis 5 à 8 exercices UNIQUEMENT dans cette liste (noms EXACTS): " + names.join(', ') + ". " +
    "Pour chaque exercice donne des séries et répétitions cohérentes avec l'objectif. " +
    "Réponds UNIQUEMENT en JSON: " +
    '{"name":"<nom du programme>","exercises":[{"name":"<nom exact de la liste>","targetSets":<n>,"targetReps":<n>,"restSeconds":<s>}]}.';

  const payload = { model: MODEL, messages: [{ role: 'user', content: prompt }], max_tokens: 600, temperature: 0.4 };
  let res;
  try {
    res = await fetch(NVIDIA_URL, {
      method: 'POST',
      headers: { Authorization: `Bearer ${API_KEY}`, 'Content-Type': 'application/json' },
      body: JSON.stringify(payload),
      signal: AbortSignal.timeout(60000),
    });
  } catch (e) {
    req.log.error({ err: String(e) }, 'NVIDIA fetch failed (generate)');
    return reply.code(504).send({ error: 'Délai dépassé côté modèle' });
  }
  if (!res.ok) {
    const t = await res.text().catch(() => '');
    req.log.error({ status: res.status, body: t.slice(0, 300) }, 'NVIDIA error (generate)');
    return reply.code(502).send({ error: 'Erreur du modèle', status: res.status });
  }
  const out = await res.json();
  const parsed = extractJson(out?.choices?.[0]?.message?.content || '');
  if (!parsed) return reply.send({ name: '', exercises: [] });
  // Validation des noms contre le catalogue
  const valid = new Set(names);
  const exercises = Array.isArray(parsed.exercises) ? parsed.exercises
    .filter((e) => e && valid.has(String(e.name).trim()))
    .map((e) => ({
      name: String(e.name).trim(),
      targetSets: Math.max(1, parseInt(e.targetSets) || 3),
      targetReps: Math.max(1, parseInt(e.targetReps) || 10),
      ...(parseInt(e.restSeconds) > 0 ? { restSeconds: parseInt(e.restSeconds) } : {}),
    })).slice(0, 12) : [];
  return reply.send({ name: typeof parsed.name === 'string' ? parsed.name.trim() : '', exercises });
});
```

- [ ] **Step 3: Syntax check** `cd /opt/apps/MuscuGain/backend && node --check server.js`

- [ ] **Step 4: Commit** `cd /opt/apps/MuscuGain && git add backend/server.js && git commit -m "feat(muscugain-backend): endpoints /coach-analysis + /generate-program (NIM texte, non médical)"`

---

### Task 3: `coach.js` IO + export catégorie

**Files:** Create `src/utils/coach.js` ; Modify `src/data/exercises.js`.

- [ ] **Step 1: Add a reverse category map in `src/data/exercises.js`**

At the end of the file, add:
```js
// nom d'exercice -> catégorie (pour les résumés/statistiques)
export const EXERCISE_CATEGORY = Object.fromEntries(
  Object.entries(EXERCISES_DB).flatMap(([cat, list]) => list.map((name) => [name, cat]))
);
export const categoryOf = (name) => EXERCISE_CATEGORY[name] || 'other';
```

- [ ] **Step 2: Create `src/utils/coach.js`**

```js
import { normalizeCoachAnalysis, normalizeGeneratedProgram } from './coach.core.js';

const MESSAGES = {
  unavailable: 'Coach IA indisponible, réessaie plus tard.',
  ratelimit: 'Trop de tentatives, réessaie dans 1 min.',
  empty: "L'IA n'a pas pu produire de résultat, réessaie.",
};

export class CoachError extends Error {
  constructor(kind, message) { super(message); this.name = 'CoachError'; this.kind = kind; }
}

async function postJson(url, body) {
  let res;
  try {
    res = await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) });
  } catch { throw new CoachError('unavailable', MESSAGES.unavailable); }
  if (res.status === 429) throw new CoachError('ratelimit', MESSAGES.ratelimit);
  if (!res.ok) throw new CoachError('unavailable', MESSAGES.unavailable);
  return res.json().catch(() => null);
}

export async function fetchCoachAnalysis(summary, bodyAnalysis) {
  const json = await postJson('/api/coach-analysis', { summary, bodyAnalysis: bodyAnalysis || null });
  const data = normalizeCoachAnalysis(json);
  if (!data.overview && !data.progression.length && !data.plateaus.length && !data.balance) {
    throw new CoachError('empty', MESSAGES.empty);
  }
  return data;
}

export async function generateProgram(objective, catalog, validNames) {
  const json = await postJson('/api/generate-program', { objective, catalog });
  const program = normalizeGeneratedProgram(json, validNames);
  if (!program.exercises.length) throw new CoachError('empty', MESSAGES.empty);
  return program;
}
```

- [ ] **Step 3: Build** `cd /opt/apps/MuscuGain && npm run build`

- [ ] **Step 4: Commit** `git add src/utils/coach.js src/data/exercises.js && git commit -m "feat(muscugain): coach.js IO + map catégorie d'exercice"`

---

### Task 4: Dashboard — carte bilan IA (cache 1/jour)

**Files:** Modify `src/views/Dashboard.jsx`, `src/App.jsx`.

**Spec :**
- App charge le bilan caché : state `coachAnalysis` depuis `localStorage.muscuGainCoachAnalysis` au mount (parse `{date,data}`). Handler `saveCoachAnalysis(data)` : `setCoachAnalysis({date: todayStr, data})` + persist (`todayStr = new Date().toISOString().slice(0,10)`). Passe `coachAnalysis`, `onCoachAnalyzed`, `bodyAnalyses` à Dashboard.
- Dashboard : nouvelle carte « Coach IA » sous la carte « Conseil du Coach » statique (garder le coach statique comme accroche rapide).
  - Si pas d'historique (`history.length === 0`) : masquer la carte IA.
  - Bouton « Demander un bilan » (icône `Sparkles`) → état loading → appelle `fetchCoachAnalysis(buildHistorySummary(history, categoryOf), bodyAnalyses[0] || null)` → `onCoachAnalyzed(data)` → affiche.
  - Si un bilan du jour est en cache (`coachAnalysis.date === today`), l'afficher direct + bouton « Rafraîchir ». Sinon proposer le bouton.
  - Affichage du bilan : `overview` en tête, puis sections compactes Progression (vert), Plateaux (amber), Volume hebdo, Équilibre, Deload, et Corps×training (`bodyCross`, si non vide). Réutiliser le style des listes de BodyAnalysis.
  - Erreur `CoachError` → message + retry. Toast non nécessaire ici.
- Imports : `buildHistorySummary`, `fetchCoachAnalysis`, `CoachError`, `categoryOf` from data/exercises, lucide `Sparkles, Loader2`.

- [ ] **Step 1: App.jsx** — add `coachAnalysis` state, mount-load, `saveCoachAnalysis`, pass props to Dashboard (`coachAnalysis`, `onCoachAnalyzed={saveCoachAnalysis}`, `bodyAnalyses`).
- [ ] **Step 2: Dashboard.jsx** — implement the Coach IA card per spec (local state for status/error, calls IO, renders).
- [ ] **Step 3: Build** `npm run build`.
- [ ] **Step 4: Commit** `git add src/views/Dashboard.jsx src/App.jsx && git commit -m "feat(muscugain): carte bilan Coach IA sur le Dashboard (cache 1/jour)"`

---

### Task 5: Génération de programme par IA

**Files:** Create `src/components/modals/GenerateProgramModal.jsx` ; Modify `src/App.jsx`, `src/views/Dashboard.jsx`.

**Spec :**
- `GenerateProgramModal` props `{ isOpen, onClose, onSave }` : textarea objectif (placeholder « Ex: prise de masse, 3 séances/semaine, niveau débutant ») → bouton « Générer » → loading → appelle `generateProgram(objective, EXERCISES_DB, Object.values(EXERCISES_DB).flat())` → aperçu (nom éditable + liste exos avec séries×reps) → boutons « Enregistrer » (→ `onSave(program)` → close) et « Régénérer ». Erreur `CoachError` → message. Style modale identique à `ImportRoutineModal`.
- App : state `generateModalOpen`; `addGeneratedRoutine(program)` : crée `{ id:'custom_'+Date.now(), name: program.name || 'Programme IA', desc:'Généré par IA', exercises: program.exercises, isCustom:true }`, prepend customRoutines + persist + `showToast('Programme généré')`. Rendu `<GenerateProgramModal isOpen onClose onSave={addGeneratedRoutine}/>`. Passe `onGenerateClick={() => setGenerateModalOpen(true)}` à Dashboard.
- Dashboard : bouton « ✨ Générer par IA » dans l'en-tête Programmes (à côté de Importer/Créer), icône `Sparkles`.

- [ ] **Step 1: Create `GenerateProgramModal.jsx`** per spec (import `generateProgram, CoachError` from `../../utils/coach`, `EXERCISES_DB` from `../../data/exercises`, Button).
- [ ] **Step 2: App.jsx wiring** (state, handler, render, prop).
- [ ] **Step 3: Dashboard.jsx** add the generate button.
- [ ] **Step 4: Build** `npm run build`.
- [ ] **Step 5: Commit** `git add src/components/modals/GenerateProgramModal.jsx src/App.jsx src/views/Dashboard.jsx && git commit -m "feat(muscugain): génération de programme par IA (modal + wiring)"`

---

### Task 6: Suggestion de charge dans Workout

**Files:** Modify `src/views/Workout.jsx`.

**Spec :** Pour chaque exercice, calculer `suggestLoad(getLastLog(exName), targetReps)` où `targetReps` = reps cible de la routine pour cet exo (sinon la `reps` de la 1ère série, sinon 8). Si non-null ET différent de la valeur saisie, afficher sous le titre de l'exercice une petite puce « Suggéré : {weight} kg » (verte si `bump`, bleue sinon) cliquable : au clic, remplit le `weight` de toutes les séries non `done` de cet exercice (via `updateSet`). Importer `suggestLoad` from `../utils/coach.core`.

- [ ] **Step 1: Implement** in `src/views/Workout.jsx` : compute target reps per exercise from `activeRoutine.exercises`, render the suggestion chip near the existing « Dernier: … » badge, add a `fillSuggestedWeight(exName, weight)` handler that sets weight on all non-done sets.
- [ ] **Step 2: Build** `npm run build`.
- [ ] **Step 3: Commit** `git add src/views/Workout.jsx && git commit -m "feat(muscugain): suggestion de charge par exercice (progression auto)"`

---

### Task 7: Deploy + e2e + docs

- [ ] **Step 1: tests + build** `cd /opt/apps/MuscuGain && node --test src/utils/ && npm run build` → pass.
- [ ] **Step 2: deploy** `docker compose up -d --build` ; wait healthy (`until docker ps --filter name=muscugain-app --format '{{.Status}}' | grep -q healthy; do sleep 3; done`).
- [ ] **Step 3: e2e** :
  - `curl -s -X POST http://127.0.0.1:8080/api/coach-analysis -H 'Content-Type: application/json' -d '{"summary":{"totalSessions":3,"perExercise":[{"name":"Squat","sessions":3,"maxWeight":110,"trend":"up"}],"muscleVolume":{"legs":1650},"weeklyFrequency":3}}' -w "\nHTTP %{http_code}\n" | head -c 400` → 200 + JSON.
  - `curl -s -X POST http://127.0.0.1:8080/api/generate-program -H 'Content-Type: application/json' -d '{"objective":"prise de masse débutant 3x/semaine","catalog":{"chest":["Développé Couché Barre"],"legs":["Squat Arrière (Back Squat)"]}}' -w "\nHTTP %{http_code}\n" | head -c 400` → 200 + exercises validés.
- [ ] **Step 4: docs** : `HISTORIQUE.MD` (entrée P2), `CLAUDE.md` (endpoints coach, utils coach, carte bilan, génération, suggestion charge ; coches P2), `ROADMAP.md` (cocher P2). Commit `docs(muscugain): P2 Coach IA livré`.

---

## Self-Review
- Bilan IA historique → Tasks 1,2,4 ✓ ; Génération programme → Tasks 2,5 ✓ ; Croisement corps×training → `/coach-analysis` `bodyAnalysis`+`bodyCross` (Tasks 2,4) ✓ ; Suggestion charge → Tasks 1,6 ✓.
- TDD core (summary/normalize/suggestLoad) → Task 1 ✓. Backend texte seul, non médical, validation noms catalogue → Task 2 ✓. Cache 1/jour → Task 4 ✓.
- **Type consistency** : `buildHistorySummary` out == body `/coach-analysis` `summary` ; `normalizeCoachAnalysis` shape == Dashboard render == cache `{date,data}` ; `normalizeGeneratedProgram` out == `addGeneratedRoutine` input (→ routine `{id,name,desc,exercises,isCustom}`) ; `suggestLoad` out `{weight,bump}` == Workout chip. ✓
- **Privé/bornes** : résumé compact (pas l'historique brut), cap 15 exos / 30 séances, objectif tronqué 300 char, noms validés contre catalogue. ✓
